import React from 'react'

const archietectjobs = () => {
    return (
        <div>
            archietectjobs
        </div>
    )
}
{
    // 1st const uId=fetch.post('/users',fnan,lname,aadhar'role'=arch, flagForAadhar = false)
    // 2nd fetch.post('/arch',uId,flagForDocs=false)
    // 3rd update api fetch.put('/arch',documents[file], photo) when he clicks on apply jobs redirect to submit the documents
}
export default archietectjobs