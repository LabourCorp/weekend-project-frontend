import React from 'react'

const contractor = () => {
    return (
        <div>contractor</div>
    )
}
{
    // 1st fetch.post('/users',fnan,lname,aadhar'role'=cont, flagForAadhar = false)
    // 2nd fetch.post('/contractor',uId,role,companyName,photo(convert to base64 and send to backend),companyAddress,noOfPeopleHandled,areaOfOperation)
}
export default contractor