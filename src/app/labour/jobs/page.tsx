import React from 'react'

const labourjobs = () => {
    return (
        <div>labourjobs</div>
    )
}
{
    // 1st fetch.post('/users',fnan,lname,aadhar'role'=lab, flagForAadhar = false)
    // 2nd fetch.post('/labour',uId,role,skills[checkbox],operatingArea[String],photo(convert to base64 and send to backend))
}
export default labourjobs